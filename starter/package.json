{
  "name": "spacta-starter",
  "version": "0.11.0",
  "private": true,
  "//scripts": [
    "The CLIs come out of node_modules, not out of a copied verify/ directory. That is the whole",
    "difference between this and the version before it: the verifier a project runs is the one",
    "that shipped with the engine it imports, so the two cannot drift apart at the version level."
  ],
  "scripts": {
    "verify": "spacta-verify .",
    "verify:tsc": "spacta-verify . --tsc",
    "garden": "spacta-garden .",
    "measure": "spacta-measure ."
  },
  "dependencies": {
    "spacta": "^0.11.0"
  },
  "devDependencies": {
    "typescript": "^5",
    "tailwindcss": "^3.4.0",
    "tailwind-variants": "^0.2.1"
  }
}
